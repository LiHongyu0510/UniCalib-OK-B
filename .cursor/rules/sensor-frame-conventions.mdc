---
description: 车体各传感器本地坐标系轴向约定（外参/坐标变换时必读）
globs:
  - calib_unified/**
  - results/**
  - merged_*/**
alwaysApply: true
---

# 传感器坐标系轴向约定

## 车体测量 V 系（安装表）

- **基点**：车头上的一个参考点（测量零点）
- **轴向（后上左）**：X = **后**，Y = **上**，Z = **左**
- 安装表里各传感器中心坐标 `(x, y, z)` mm 均在此 **同一 V 系** 下给出；相对位置 `delta_V = r_lidar - r_cam`

---

各传感器 **本地坐标系** 的 X/Y/Z 轴方向（按轴顺序写出，即 X→Y→Z 分别对应下列第一/二/三方向）。

## LiDAR

| sensor_id   | X      | Y      | Z      | 记法   |
|-------------|--------|--------|--------|--------|
| lidar_main  | 前     | 左     | 上     | 前左上 |
| left_front  | 前     | 右     | 下     | 前右下 |
| right_back  | 右     | 后     | 下     | 右后下 |
| left_back   | 左     | 前     | 下     | 右前下 |
| right_front | 前     | 右     | 下     | 前右下 |

## IMU

| sensor_id | X      | Y      | Z      | 记法   |
|-----------|--------|--------|--------|--------|
| imu_0     | 右     | 前     | 上     | 右前上 |

## Camera（外参标定将用到）

| sensor_id          | X      | Y      | Z      | 记法   |
|--------------------|--------|--------|--------|--------|
| center_front       | 右     | 下     | 前     | 右下前 |
| surrounding_front  | 右     | 下     | 前     | 右下前 |
| surrounding_left   | 前     | 下     | 左     | 前下左 |
| surrounding_right  | 后     | 下     | 右     | 后下右 |
| surrounding_back   | 左     | 下     | 后     | 左下后 |

## 使用说明

- 外参矩阵表示 **目标传感器坐标系 → 源传感器坐标系**（或标定输出约定）时，必须先确认两端各自的轴向，再构造/校验旋转。
- 不同传感器之间轴向不一致是常态；**不可**假设所有传感器与车体或彼此同向。
- 中文记法：「前左上」= X 指向前、Y 指向左、Z 指向上。
